import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /** Hides the corner dev route indicator (e.g. “N” / devtools-indicator) in development. */
  devIndicators: false,
};

export default nextConfig;
